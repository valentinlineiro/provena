{
  "name": "@provena/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@provena/core": "*",
    "@provena/market-postgres": "*",
    "@provena/markdown": "*",
    "@provena/html": "*",
    "postgres": "^3.4.4",
    "typescript": "^5.7.0",
    "wrangler": "^4.115.0"
  }
}
